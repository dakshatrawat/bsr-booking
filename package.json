{
  "name": "bsr-booking",
  "version": "1.0.0",
  "description": "Hotel booking system - Root package for Vercel deployment",
  "scripts": {
    "build": "cd angular-frontend && npm install && npm run build:prod"
  },
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  }
}

